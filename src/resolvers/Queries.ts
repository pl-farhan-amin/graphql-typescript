import { hello, game, author, review, getReviewById,getGameById,getAuthorById } from "../queries";

const Query = {
    hello,
    game,
    author,
    review,
    getReviewById,
    getGameById,
    getAuthorById
}

export default Query;